fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pinned: passlib 1.7.4 has a documented incompatibility with bcrypt>=4.1
# (reads an internal attribute bcrypt removed), which can make password
# hashing/verification fail at runtime with a confusing error.
bcrypt==4.0.1
python-multipart==0.0.9
httpx==0.27.2
python-dotenv==1.0.1

# --- testing ---
pytest==8.3.3
